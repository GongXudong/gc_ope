# GC-OPE 项目初读理解

## 0. 文档目的

本文是对当前实验目录的第一次结构化理解，供项目作者逐环节检查。文中区分“代码直接体现的事实”和“基于代码的解释”；若有理解偏差，请优先指出对应环节。

## 1. 项目要估计什么

项目研究的是 goal-conditioned policy 的离策略评估（OPE）。给定策略 \(\pi\) 和目标分布 \(p_{dg}\)，总体目标应可写成

\[
J(\pi)=\mathbb E_{g\sim p_{dg}}[V^\pi(g)],
\]

其中 \(V^\pi(g)\) 表示策略在目标 \(g\) 下的表现。当前代码中的主要可观测表现是 episode success，也同时记录累计奖励、折扣累计奖励和 episode length。因此实际实验可能同时关心：平均成功率、目标级成功能力分布，以及 reward 类指标。

这里需要作者确认：最终论文/实验所定义的 \(J(\pi)\) 究竟是 success probability、累计 reward，还是某种目标空间上的综合指标。代码的保存模型逻辑明确偏向 success rate。

## 2. 为估计准备什么数据

### 2.1 目标级评估数据

周期性 evaluation 运行当前策略，针对一批 desired goals 生成 episode。每条评估记录包含：

- `desired_goal`：本 episode 的目标；
- `success`：是否达到目标；
- `cumulative_reward`；
- `discounted_cumulative_reward`。

这些记录由 callback 产生，并同步到训练环境中的 `SyncEvaluationResultWrapper`。

### 2.2 三套环境

训练使用 `train_env`；独立最终评估使用 `eval_env`；callback 周期评估使用 `callback_env`。课程学习 wrapper 只包在训练环境上，但它接收 callback 的评估统计。

### 2.3 时间衰减

`WeightedEvaluationResultContainer` 对旧数据的目标权重乘以 `discounted_factor`，新评估样本权重为 1。因此 KDE 主要反映近期策略能力，同时保留历史信息。

## 3. 基于什么方法估计

当前实现使用 KDE（Kernel Density Estimation）。它筛选所有成功评估的目标：

\[
\mathcal D^+ = \{g_i:\text{success}_i=1\}.
\]

成功目标先标准化，再用带时间权重的 KDE 拟合：

\[
\hat p_{ag}(g) = \operatorname{KDE}(\mathcal D^+).
\]

因此 KDE 直接估计的是“策略成功达到的目标在目标空间中的经验密度”，不是严格意义上的条件成功概率 \(P(\text{success}\mid g)\)。失败样本没有作为 KDE 的负样本参与拟合。

## 4. 得到什么估计输出

KDE 对历史成功目标输出密度，对候选目标输出 density score。该 score 用于：

- 判断候选目标是否落在当前成功分布的支撑区域；
- 找到当前成功区域中的低密度区域；
- 作为 RIG、DISCERN、MEGA 的目标采样依据；
- 与均匀目标分布比较，近似计算 OMEGA 所需的 KL 散度。

这里的 OPE 输出更像目标级能力模型 \(\hat p_{ag}\)，而不是一个单独的标量 \(\hat J(\pi)\)。若要得到 \(\hat J(\pi)\)，可以在目标分布上对目标级成功估计积分/平均；当前目录中需要确认是否已有独立代码完成这一步。

## 5. 估计如何反过来改变训练

### 5.1 无成功数据

如果尚无成功评估目标，wrapper 退回环境原本的随机 desired-goal 采样。

### 5.2 MEGA / RIG / DISCERN

每次 reset 时先从环境目标空间采样 `sample_N` 个候选目标，再用 KDE 评分：

- `rig`：按密度加权采样高密度目标；
- `discern`：过滤低于阈值的目标后均匀采样；
- `mega`：选择仍在阈值以上但密度最低的目标，推动当前可达区域内部的覆盖扩张。

阈值取历史成功目标 KDE 密度的最小值。若候选目标全部低于阈值，则回退到随机候选或最高密度候选，具体由方法实现决定。

### 5.3 OMEGA

OMEGA 估计目标空间均匀分布与 \(\hat p_{ag}\) 的 KL，并据此计算随机探索概率 \(\alpha\)。以概率 \(\alpha\) 使用原始随机目标，以概率 \(1-\alpha\) 使用 MEGA/RIG/DISCERN 采样。

## 6. 训练算法与实验因素

训练主干由配置选择 PPO、SAC 或 SAC+HER。环境包括 Reach、Push、Slide、PointMaze、AntMaze、FlyCraft。实验因素至少包括：

- 算法：PPO / SAC / HER；
- 是否使用课程学习；
- 课程方法：MEGA / OMEGA，以及 RIG / DISCERN 采样变体；
- 目标评估集合：随机目标或固定目标集合；
- 环境任务和随机种子。

## 7. 我目前理解的完整闭环

\[
\text{策略 }\pi
\rightarrow \text{在目标集合上评估}
\rightarrow \text{收集 }(g,\text{success},r)
\rightarrow \text{筛选成功目标}
\rightarrow \hat p_{ag}
\rightarrow \text{估计候选目标难度/覆盖情况}
\rightarrow \text{改变目标采样}
\rightarrow \text{继续训练 }\pi
\rightarrow \text{重新评估}.
\]

若把最终 OPE 写成用户提示中的形式，我目前的对应关系是：

\[
J(\pi)
\rightarrow \text{目标级评估统计}
\rightarrow \text{KDE 或目标空间积分/平均}
\rightarrow \hat J(\pi).
\]

但当前实现中 KDE 更直接产生 \(\hat p_{ag}\)，而 \(\hat J(\pi)\) 的精确定义和计算位置仍需作者确认。

## 8. 希望作者重点检查的疑问

1. \(J(\pi)\) 的正式定义和最终报告指标是什么？
2. 项目中的 OPE 是否有独立的标量 \(\hat J(\pi)\)，还是将 \(\hat p_{ag}\) 本身作为主要估计对象？
3. KDE 的成功目标密度是否被理论上解释为可达目标分布，还是仅作为课程采样启发式？
4. `discounted_factor` 的设计意图是时间衰减、滑动窗口近似，还是别的统计权重？
5. OMEGA 中 KL 的方向、积分近似和 \(\alpha\) 公式是否与实验设定中的理论定义完全一致？
6. random/fixed goal evaluation 分别承担泛化评估和可重复对比中的哪一种角色？

## 9. 从实验产物目录反向理解实验矩阵

另一种理解方式是把 `checkpoints/` 看作实验的结果索引。其典型层级为：

```text
checkpoints/
  <env_str>/
    <method-or-algorithm>/
      seed_<n>/
        rl_model_<timesteps>_steps.zip
        best_model.zip
        *_eval_res_on_random.csv
        *_eval_res_on_fixed.csv
        *_replay_buffer_*.pkl
```

因此每个目录首先回答“在哪个环境、用哪种训练方法、哪个随机种子”，文件名再回答“训练到多少步、是否为最佳模型、是否保存了评估结果或 replay buffer”。

`rl_model_<timesteps>_steps.zip` 是某个训练时刻的策略 checkpoint；对应的 `*_eval_res_on_random.csv` 和 `*_eval_res_on_fixed.csv` 是该 checkpoint 在随机目标集合和固定目标集合上的逐目标评估记录。前者适合观察目标分布上的总体泛化，后者适合不同 checkpoint、方法和 seed 之间的可重复横向比较。`best_model.zip` 则是 callback 按 success rate 保存的阶段性最佳策略。

带有 `replay_buffer` 的 pkl 文件不是策略本身，而是 SAC/HER 等 off-policy 训练所需的经验回放状态；它支持从某个训练阶段继续训练或进行与 replay buffer 相关的实验对照。PPO 通常不需要这类持久化回放文件。

从 `scripts/train_policy/shells/` 可以看出，实验矩阵主要沿以下维度展开：环境（Reach、Push、Slide、PointMaze、AntMaze、FlyCraft）、算法（PPO、SAC、HER）、课程方法（原始随机目标、MEGA、OMEGA，以及若干 replay-buffer / ODISCERN 变体）、随机种子和训练步数。`scripts/evaluate_ckpt/shells/` 则把已训练 checkpoint 作为输入，在统一评估配置下批量生成 CSV，形成“训练一次、多个时刻和多个目标集合反复评估”的工作流。

所以从产物角度，单个实验单元可以理解为：

\[
\text{环境} + \text{算法/课程配置} + \text{seed}
\rightarrow \text{训练轨迹上的一串 checkpoint}
\rightarrow \text{每个 checkpoint 的 random/fixed 目标评估结果}
\rightarrow \text{success 分布、KDE/OPE 指标和方法比较}.
\]

需要作者确认的是：目录名中的 `orig`、`omega`、`mega`、`odiscern` 是否严格对应论文中的方法定义，还是包含过渡性实现/消融实验；以及最终表格或图像究竟选用 `best_model`、固定训练步数 checkpoint，还是两者都报告。

## 10. 重点实验线：MyPush/SAC 与 MySlide/SAC

这两条线的目录结构几乎平行，区别主要来自任务动力学：Push 是推动物体到目标，Slide 是使滑块到目标；二者都使用稀疏奖励，且 Slide 明确不惩罚速度。

### 10.1 实验命名

`mega_dscnt_0_9_n_100_eval_96_seed_3` 表示 SAC+MEGA，历史评估权重折扣 0.9，每次 reset 采样 100 个候选目标，callback 每轮评估 96 个 episode，第 3 个随机种子。`omega_dscnt_0_9_b_0_n_100_eval_96_seed_3` 则是在此基础上使用 OMEGA，`b=0`；`odiscern_*` 仍使用 OMEGA wrapper，但局部采样方法改为 DISCERN。

`orig_*` 是原始随机目标采样的 SAC 对照；`*_rb_*` 是使用 `*_replay_buffer` callback、从 SAC replay buffer 抽取样本来构造或刷新评估统计的版本。普通 `seed_<n>` 目录是最基础的 vanilla SAC 训练结果。

### 10.2 MEGA 与 OMEGA 设置

MEGA 根据近期成功目标的 KDE 密度，选择成功支撑内的低密度目标。OMEGA 额外计算均匀目标分布与 KDE 分布的 KL，并以 `alpha` 混合全局随机目标与局部课程采样。Push 和 Slide 的主设置通常是折扣因子 0.9、候选数 100、callback 评估 96、五个 seed。

### 10.3 Push 上的消融实验

Push 目录系统改变四类因素：`dscnt_0_8/0_9/0_95/0_99` 是历史数据折扣因子；`b_-3/-2/-1/0/0_5` 是 OMEGA 的 `b_used_in_omega`；`n_10/n_20/n_100` 是候选目标数；`eval_16/eval_32/eval_96/eval_288` 是 callback 每轮评估量。这些是同一 SAC+OMEGA 机制下的数据新鲜度、探索偏置、候选搜索预算和统计量规模的消融，不是四种新算法。

`evaluate_ckpt/shells/my_push/ablations/` 对这些 checkpoint 统一生成两类结果：随机目标评估（通常 1000 个目标）和 fixed 目标网格评估（x/y/z 间隔 0.02），分别观察总体泛化与可重复的目标空间覆盖。

### 10.4 Slide 上的对应实验

`my_slide/sac` 中的 `orig`、`mega`、`omega`、`odiscern`、`*_rb_*`、折扣因子、候选数、评估量和 seed 的含义与 Push 相同。它的作用是把同一套 SAC curriculum/OPE 机制迁移到滑块任务，检验结论是否依赖 Push 的接触动力学。`back` 子目录看起来是历史或回退产物，不能仅凭目录名视为正式主实验。

### 10.5 一个目录的完整读法

`my_push/sac/omega_dscnt_0_9_b_0_n_100_eval_96_seed_3` 可展开为：

```text
MyPushSparse-v0 + SAC + OMEGA
+ 局部采样方法 MEGA + KDE 折扣 0.9
+ b=0 + 每次 100 个候选目标
+ callback 每轮 96 个评估 episode + 第 3 个 seed
```

训练产生一串 checkpoint；每个 checkpoint 再被 random/fixed 评估脚本读取，最终比较的是目标空间上的 success 覆盖与泛化，而不仅是 reward 曲线。

## 11. 如何判断每个 checkpoint 是怎样得到的

不能单看末级目录名 `seed_1` 到 `seed_5` 判断是否使用课程学习。`seed_1~seed_5` 只是同一实验设置下的五次独立重复；真正决定训练机制的是它们所在的父目录。

例如：

```text
checkpoints/my_push/sac/seed_1
```

对应 `scripts/train_policy/shells/my_push/vanilla/sac.sh`，使用 `env=my_push`，其中 `use_curriculum: false`，因此这是没有课程学习的 vanilla SAC 训练轨迹。`seed_1` 到 `seed_5` 是五次不同随机种子的 vanilla SAC。

而：

```text
checkpoints/my_push/sac/omega_dscnt_0_9_b_0_n_100_eval_96_seed_1
```

对应 `scripts/train_policy/shells/my_push/curriculum/sac_omega.sh`，使用 `env=my_push_omega` 和 `callback=sac_omega`，因此这是 SAC+OMEGA 的第 1 次重复；同目录下的 checkpoint 是该次 OMEGA 训练在不同 timestep 保存的策略。

相同规则适用于 Push 和 Slide：

| checkpoint 父目录 | 背后训练过程 |
|---|---|
| `sac/seed_<n>` | vanilla SAC，随机目标，无 curriculum |
| `sac/orig_*_seed_<n>` | 原始随机目标 SAC 的命名实验，通常带特定 callback/统计设置 |
| `sac/mega_*_seed_<n>` | SAC+MEGA；KDE 统计驱动目标采样 |
| `sac/omega_*_seed_<n>` | SAC+OMEGA；KL 调节随机探索与局部采样 |
| `sac/odiscern_*_seed_<n>` | OMEGA wrapper + DISCERN 局部采样 |
| `sac/*_rb_*_seed_<n>` | 对应方法使用 replay buffer 统计 callback |

因此每个具体 checkpoint 的来源应沿四层追溯：

```text
环境目录（my_push / my_slide）
→ 算法目录（sac）
→ 实验设置目录（vanilla / orig / mega / omega / odiscern，以及参数）
→ seed 编号
→ timestep checkpoint
```

例如 `rl_model_300000_steps.zip` 不是一条独立实验，而是上述某个“环境 + SAC 设置 + seed”训练到 300000 个环境步时保存的策略快照。它与同目录下 `rl_model_400000_steps.zip` 的差别主要是训练进度；与另一个设置目录下同名 timestep checkpoint 的差别则是训练机制。

`best_model.zip` 也来自同一条训练轨迹，只是在 callback 评估时按 success rate 选出的当前最佳策略。对应 CSV 是对某个 checkpoint 运行独立评估得到的结果，不参与生成该 checkpoint 本身。

## 12. 与旧版论文的对齐：项目真正的论文主线

阅读 `paper/ICML2026/` 后，项目的核心方法应称为 **PE-GCRL (Periodic Evaluation for GCRL)**。它不是提出一个全新的 RL optimizer，而是在通用 GCRL 的 `TRAIN` 循环中加入 `EVALUATE`，并修改 `SELECT`：

```text
TRAIN:
  SELECT behavioral goal
  ROLLOUT 收集 RB
  RELABEL（HER 可选）
  OPTIMIZE 更新 policy
  PERIODIC EVALUATE 当前 policy
  将评估结果写入 evaluation result buffer
```

论文把三类 goal 分得很清楚：`desired goal` 来自环境目标分布 (p_{dg})；`behavioral goal` 是实际 rollout 时用于采集训练数据的目标；`achievable goal` 是当前 policy 能够达到的目标，其分布记为 (p_{ag})。课程学习的直接目的，是根据对 (p_{ag}) 的估计选择 GOIDs（Goals of Intermediate Difficulty）。

论文中选择二元 success 作为 goal achievement capability 的度量，而不是直接估计 expected return。理由是 success 的变化更平滑、估计方差更小，并且容易用于边界目标选择。PE-GCRL 的 practical implementation 维护历史评估元组 \((g_i,s_i,t_i)\)，在当前时刻用 \(\kappa^{k-t_i}\) 对历史结果加权，并只用 success 样本拟合 \(\tilde p_{ag}\)；代码中的 `discounted_factor` 就对应论文的 \(\kappa\)。

## 13. 论文实验名与代码目录名的对应

论文主实验真正比较的是：

- **GC-SAC**：SAC 直接做 GCRL，behavioral goal 只来自 (p_{dg})，无 curriculum；
- **OMEGA-RB**：用 replay buffer 中的数据估计 \(\tilde p_{ag}\)，再按 OMEGA 采样 behavioral goal；
- **OMEGA-PE**：用 historical evaluation results 估计 \(\tilde p_{ag}\)，其余 OMEGA 机制不变，是论文提出的 PE-GCRL 实现。

对应到当前 Push/Slide 的 SAC 目录：

```text
sac/seed_<n>       ≈ GC-SAC / vanilla SAC
sac/*_rb_*         ≈ OMEGA-RB 或其 replay-buffer 统计变体
sac/omega_*        ≈ OMEGA-PE（历史 evaluation 统计）
sac/odiscern_*     ≈ 使用 DISCERN 作为局部采样规则的 OMEGA 变体
```

这里必须以训练 shell 中的 `callback` 为最终判据：`sac_omega` 是 evaluation 统计同步到 wrapper 的 PE 路线；`sac_omega_replay_buffer` 才是从 replay buffer 抽取样本的 RB 路线。目录名里的 `omega` 本身不能单独证明使用了哪类估计数据。

旧论文的关键实验证据包括：相邻训练时刻的 (p_{ag}) 差异比 (J(\pi)) 的差异更平滑；历史 evaluation 数据越接近当前时刻，越能有效估计当前 (p_{ag})；OMEGA-PE 在 Reach、Push、FlyCraft 上通常提升收敛速度或最终 success，Slide 上主要达到与 baseline 相近的最终水平；额外时间开销通常低于 5%。

论文还明确了四类消融：评估 episode 数 (N_e)、历史权重折扣 \(\kappa\)、候选 behavioral goal 数 (N)、OMEGA 平衡因子 (b)。当前目录中的 `eval_*`、`dscnt_*`、`n_*`、`b_*` 正对应这些消融轴。

## 14. 对“重新投稿扩充实验”的当前理解

因此，现阶段不是重新定义一个完全不同的课题，而是在已有 PE-GCRL 论文上扩充证据和实验边界。最自然的扩展方向是：更系统地比较 PE 与 RB 的估计误差，拆开 OMEGA/MEGA/DISCERN 的作用，扩大 Push/Slide 的超参数和 seed 实验，并进一步报告 success 之外的 reachability、horizon 或更细粒度的 (J(\pi)) 估计。论文自己也把二元 success、在线评估成本和高维 KDE 可扩展性列为限制，这些可作为重新投稿时的实验扩展依据。

## 15. 修正后的实验组织层级

项目实验按“场景 → 强化学习算法 → 基础课程学习算法/无课程 → O优化版本 → 参数 → seed → checkpoint”理解。场景包括 MyReach、MyPush、MySlide、MyPointMaze、MyAntMaze、FlyCraft；强化学习算法主要是 PPO、SAC、HER（HER 实现为 SAC+HerReplayBuffer）。

基础课程学习算法是四种并列选项：无课程、MEGA、DISCERN、RIG。MEGA、DISCERN、RIG 是三个独立算法。`O` 是额外优化机制，因此 OMEGA、ODISCERN、ORIG 应分别读作：

```text
OMEGA = O + MEGA
ODISCERN = O + DISCERN
ORIG = O + RIG
```

目录名中的 `dscnt`、`b`、`n`、`eval` 是固定算法组合下的参数设置：历史评估权重折扣、O 优化中的平衡/进度参数、候选 behavioral goal 数、callback 评估 episode 数。`seed_1` 到 `seed_5` 是同一设置的五次独立重复，不是五种算法。每个 seed 目录下的 timestep 文件是同一条训练轨迹的策略快照；`best_model.zip` 是按 success rate 选出的最佳快照；CSV 是对快照进行 random/fixed desired-goal 评估后的结果。

例如 `my_push/sac/omega_dscnt_0_9_b_0_n_100_eval_96_seed_3` 应读作：MyPush 场景、SAC、MEGA 加 O 优化（OMEGA）、discount=0.9、b=0、候选数 100、评估量 96、第 3 个独立 seed。`my_push/sac/seed_1` 则是 MyPush 上不使用课程学习的 vanilla SAC 第 1 次重复。

比较实验时，应先固定场景和强化学习算法，再比较无课程、MEGA、DISCERN、RIG及其 O 优化版本，最后在每个实验组内汇总五个 seed。
